node_modules
# build/ is committed so the package can be consumed as a git dependency
# (mirrors @omnidotdev/garden-canvas); only the heavy sourcemaps are ignored
build/**/*.map
build/**/*.tsbuildinfo
